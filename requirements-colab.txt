# Colab keeps a CUDA-enabled PyTorch build in the runtime image. Do not install
# torch/torchvision here, and avoid old hard pins that downgrade Colab packages.
PyYAML>=6.0.1,<7
tqdm>=4.66,<5
wandb>=0.17,<1
pandas>=2.2,<3
tabulate>=0.9,<1
matplotlib>=3.9,<4
pywavelets>=1.6,<2
scipy>=1.14,<2
Pillow>=10.4,<13
sympy>=1.13,<2
