base_config: configs/kvasir_wbsnet.yaml

experiment:
  name: kvasir_a3_lfsa_only
  tags: ["ablation", "A3", "lfsa-only"]

model:
  use_wavelet: true
  use_lfsa: true
  use_hfba: false
  boundary_supervision: false
